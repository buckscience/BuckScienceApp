@{
    ViewData["Title"] = "Account Settings";
}

<div class="container-fluid h-100 d-flex flex-column">
    <div class="d-flex flex-column h-100">
        <h2 class="sidebar-title">Account Settings</h2>
        <hr class="sidebar-title-separator" />
        
        <div class="settings-content flex-grow-1">
            
            <!-- Settings Placeholder -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-gear me-2"></i>Settings
                    </h5>
                </div>
                <div class="card-body">
                    <p class="text-muted">Account settings will be available here in a future update.</p>
                    
                    <div class="mt-3">
                        <a href="/account" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Back to Account
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>